# Automatically added by dh_installinit
if [ -x "/etc/init.d/dsctl-virt-detect" ]; then
	update-rc.d dsctl-virt-detect defaults >/dev/null
	invoke-rc.d dsctl-virt-detect start || exit $?
fi
# End automatically added section
